[variables]
main_domain = "${domain}"

[config]
mounts = []

[[config.domains]]
serviceName = "commafeed"
port = 8082
host = "${main_domain}"

[config.env]
# Database Configuration
POSTGRES_USER  = "commafeed"
POSTGRES_PASSWOR = "${password:32}"
POSTGRES_DB = "commafeed"

#default id and password
#ID: admin
#Pass: admin

# Admin Configuration
COMMAFEED_ENABLE_ADMIN_REGISTRATION = "${enable_admin_registration}"