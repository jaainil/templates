version: "3.8"
services:
  commafeed:
    image: athou/commafeed:latest-h2
    restart: unless-stopped
    volumes:
      - commafeed-data:/commafeed/data
    deploy:
      resources:
        limits:
          memory: 256M
volumes:
  commafeed-data: {}
