version: "3.8"
services:
  dumbbudget:
    image: dumbwareio/dumbbudget:latest
    restart: unless-stopped
    ports:
      - 3000
    volumes:
      - dumbbudget_data:/app/data
volumes:
  dumbbudget_data: {}
