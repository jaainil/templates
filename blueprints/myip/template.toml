[variables]
main_domain = "${domain}"

[config]
mounts = []

[[config.domains]]
serviceName = "myip"
port = 18966
host = "${main_domain}"

[config.env]
# No additional environment variables required for MyIP service 