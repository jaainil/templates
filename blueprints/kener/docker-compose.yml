version: "3.8"
services:
  kener:
    image: rajnandan1/kener:latest
    restart: unless-stopped
    ports:
      - 3000
    volumes:
      - data:/app/database
      - ./uploads:/app/uploads

  # Uncomment the following if using PostgreSQL instead of SQLite
  # postgres:
  #   image: postgres:alpine
  #   restart: unless-stopped
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data

  # Uncomment the following if using MySQL instead of SQLite
  # mysql:
  #   image: mariadb:11
  #   restart: unless-stopped
  #   volumes:
  #     - mysql-data:/var/lib/mysql

volumes:
  data:
  # postgres-data:
  # mysql-data:
