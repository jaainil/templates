[variables]
PUBLIC_HTTPS_ONLY = "false"
PUBLIC_SIGNUP_DISABLED = "false"
main_domain = "${domain}"

[config.env]
PUBLIC_HTTPS_ONLY = "${PUBLIC_HTTPS_ONLY}"
PUBLIC_SIGNUP_DISABLED = "${PUBLIC_SIGNUP_DISABLED}"

[config]
mounts = [
  { source = "grimoire_data", target = "/app/data/" }
]

[[config.domains]]
serviceName = "grimoire"
port = 5173
host = "${main_domain}"
