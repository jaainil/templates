version: "3.8"
services:
  opengist:
    image: ghcr.io/thomiceli/opengist:1.10
    restart: unless-stopped
    ports:
      - 6157
      - 2222 # Comment out if SSH is not needed
    volumes:
      - opengist-data:/opengist

volumes:
  opengist-data:
