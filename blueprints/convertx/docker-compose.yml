version: "3.8"
services:
  convertx:
    image: ghcr.io/c4illin/convertx:latest
    restart: unless-stopped
    ports:
      - 3000
    volumes:
      - convertx-data:/app/data

volumes:
  convertx-data:
