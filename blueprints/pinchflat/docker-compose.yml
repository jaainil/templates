services:
  pinchflat:
    image: ghcr.io/kieraneglin/pinchflat:latest
    restart: unless-stopped
    ports:
      - 8945
    environment:
      - TZ=${TZ}
    volumes:
      - config_data:/config
      - downloads_data:/downloads

volumes:
  config_data:
  downloads_data:
