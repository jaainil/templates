version: "3.8"
services:
  dumbassets:
    image: dumbwareio/dumbassets:latest # Replace with build: . for local builds
    restart: unless-stopped
    ports:
      - 3000
    volumes:
      - dumbassets_data:/app/data
volumes:
  dumbassets_data: {}
