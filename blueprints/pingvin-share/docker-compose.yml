version: "3.8"
services:
  pingvin-share:
    image: ghcr.io/stonith404/pingvin-share:latest
    restart: unless-stopped
    ports:
      - 3000
    volumes:
      - pingvin-data:/opt/app/backend/data
      - pingvin-images:/opt/app/frontend/public/img
      # - pingvin-config:/opt/app/config.yaml:ro  # Uncomment for custom config.yaml
