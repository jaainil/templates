version: "3.8"
services:
  metube:
    image: ghcr.io/alexta69/metube:latest
    restart: unless-stopped
    ports:
      - 8081
    volumes:
      - metube-downloads:/downloads
      # - metube-ssl-crt:/ssl/crt.pem  # Uncomment for HTTPS
      # - metube-ssl-key:/ssl/key.pem  # Uncomment for HTTPS

volumes:
  metube-downloads:
  # metube-ssl-crt:  # Uncomment for HTTPS
  # metube-ssl-key:  # Uncomment for HTTPS
