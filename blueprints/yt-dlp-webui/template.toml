[variables]
main_domain = "${domain}"

[config.env]
YTDLP_WEBUI_VERSION = "latest"

[config]
mounts = [
  { source = "ytdlp_downloads", target = "/downloads" },
  { source = "ytdlp_config", target = "/config" }
]

[[config.domains]]
serviceName = "yt-dlp-webui"
port = 3033
host = "${main_domain}"
